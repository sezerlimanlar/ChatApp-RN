import { FlatList, StyleSheet, Text, View } from 'react-native'
import React from 'react'

export default function RoomList() {
  return (
    <FlatList data={null} renderItem={nulll} keyExtractor={null}/>
  )
}

const styles = StyleSheet.create({})